document.title;// Title of current document
document.lastModified;// Date on w hich document was last modified
document.URL;// Returns string containing URL of current document
document.domain;// Returns domain of current document

document.write();// Writes text to document (see restrictions on p226)
document.getElementByld();// Returns element, if there is an element with the value of the id attribute that matches;
document.querySe1ectorA11();// Returns list of elements that match a CSS selector, which is specified as a parameter each element on the page.

document.createElement();// Creates new element (see p222)
document.createTextNode();// Creates new text node (see p222)